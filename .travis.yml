language: bash

sudo: required

services:
    - docker

env:
    - VERSION=5.6-sybase16

install:
    - git clone --depth=50 --branch=${VERSION} https://github.com/cbsan/docker-php.git cbsan/docker-php

script:
    - docker build -t cbsan/php:${VERSION} .

after_success:
    - docker login -u="$DOCKER_USER" -p="$DOCKER_PASS" -e="$DOCKER_EMAIL"
    - docker push cbsan/php:${VERSION}
  
